// Assigned to _maxStudentsSize after accepting it.
export default class ClassRoom {
  /**
   * Has a class named ClassRoom
   * @param {Number} maxStudentsSize - Is max no. of students.
   */
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}
